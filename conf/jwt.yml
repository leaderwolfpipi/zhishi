# jwt-token配置
token:
    # token生成秘钥
    signKey: '82040620FEFAC4511FC65000ADAB0F77'
    # 签名生效时间单位秒（配置负数表示立即生效，正数表示延迟生效)
    activeTime: '-10s'
    # token有效期单位天（只能为正数）
    expiredTime: '3d'
    # 刷新token有效时间
    refreshExpiredTime: '30d'
    # 发行人
    issuer: 'bsit'