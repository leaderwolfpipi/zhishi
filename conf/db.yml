# db连接相关配置
# mysql连接配置
mysql:
    driver: mysql
    url: '@tcp(127.0.0.1:3306)/zhishi?charset=utf8&parseTime=True&loc=Local'
    username: root
    password: 123456
    max_open_conns: 100
    max_idle_conns: 10
    # 表名不可为复数形式
    singular: true
	
# oracle连接配置