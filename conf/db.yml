# db连接相关配置
# mysql连接配置
mysql:
    driver: mysql
    url: '@tcp(127.0.0.1:3306)/zhishi?charset=utf8&parseTime=True&loc=Local'
    username: root
    password: 123456
    maxOpenConns: 100
    maxIdleConns: 10
    # 表名不可为复数形式
    singular: true

# oracle连接配置